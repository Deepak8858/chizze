---
phase: 02-clear-stuck-orders-by-deleting-them-from-the-server
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - admin/types/index.ts
  - admin/lib/api.ts
  - admin/app/(admin)/orders/page.tsx
  - admin/app/(admin)/orders/page.test.tsx
autonomous: true
requirements:
  - AOC-04
  - AOC-05
must_haves:
  truths:
    - Admin can preview stale cleanup candidates before deleting.
    - Admin must pass an explicit confirmation step before bulk deletion is enabled.
    - Admin sees clear deletion result counts and refreshed order list after cleanup.
  artifacts:
    - path: admin/lib/api.ts
      provides: Typed preview and bulk-delete order cleanup API methods.
    - path: admin/types/index.ts
      provides: Cleanup preview and delete result contracts for admin UI.
    - path: admin/app/(admin)/orders/page.tsx
      provides: Guarded cleanup panel integrated into orders list workflow.
    - path: admin/app/(admin)/orders/page.test.tsx
      provides: Regression coverage for confirmation gating and mutation payloads.
  key_links:
    - from: admin/lib/api.ts
      to: backend/cmd/server/main.go
      via: ordersApi.previewStuck and ordersApi.deleteStuck mapped to new backend routes.
    - from: admin/app/(admin)/orders/page.tsx
      to: admin/lib/api.ts
      via: React Query preview query and delete mutation.
    - from: admin/app/(admin)/orders/page.test.tsx
      to: admin/app/(admin)/orders/page.tsx
      via: Mocked query/mutation assertions for guarded cleanup behavior.
---

<objective>
Ship a guarded admin cleanup experience on the Orders page that previews stale candidates, requires explicit confirmation, and executes bulk deletion safely.

Purpose: Prevent accidental destructive actions while giving admins a reliable way to clear stale server-side orders.
Output: Typed API integration, UI confirmation workflow, and focused admin regression tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-clear-stuck-orders-by-deleting-them-from-the-server/02-RESEARCH.md
@.planning/phases/02-clear-stuck-orders-by-deleting-them-from-the-server/02-01-PLAN.md
@admin/types/index.ts
@admin/lib/api.ts
@admin/app/(admin)/orders/page.tsx
@admin/app/(admin)/orders/[id]/page.test.tsx

<interfaces>
From admin/types/index.ts:
```ts
export type OrderStatus =
  | "placed" | "confirmed" | "preparing" | "ready"
  | "pickedUp" | "outForDelivery" | "delivered" | "cancelled";

export interface Order {
  $id: string;
  order_number: string;
  status: OrderStatus;
  placed_at: string;
  grand_total: number;
}
```

From admin/lib/api.ts:
```ts
export const ordersApi = {
  list: (p?: Record<string, unknown>) => GET("/admin/orders", p),
  get: (id: string) => GET(`/admin/orders/${id}`),
  getActive: (p?: Record<string, unknown>) => GET("/admin/orders/active", p),
};
```

From backend cleanup contract (plan 02-01 target):
```ts
GET /admin/orders/stuck/preview
POST /admin/orders/stuck/delete
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed admin cleanup API contracts and client methods</name>
  <files>admin/types/index.ts, admin/lib/api.ts</files>
  <read_first>
    - admin/types/index.ts
    - admin/lib/api.ts
    - .planning/phases/02-clear-stuck-orders-by-deleting-them-from-the-server/02-01-PLAN.md
  </read_first>
  <behavior>
    - Admin code has explicit types for preview filters and delete result counters.
    - ordersApi exposes preview and delete methods for the new backend endpoints.
    - Existing ordersApi methods remain backward-compatible.
  </behavior>
  <action>
    Extend admin/types/index.ts with cleanup-specific contracts (preview filters, preview response payload rows, and delete result counters/failure details). Update admin/lib/api.ts `ordersApi` with `previewStuck` and `deleteStuck` methods mapped to GET /admin/orders/stuck/preview and POST /admin/orders/stuck/delete. Ensure status array filters are serialized consistently (comma-joined query param or equivalent stable shape), and keep current list/get/active methods unchanged.
  </action>
  <acceptance_criteria>
    - admin/types/index.ts includes cleanup preview and delete result interfaces.
    - admin/lib/api.ts exports ordersApi.previewStuck and ordersApi.deleteStuck.
    - Existing ordersApi.list/get/getActive signatures still exist.
  </acceptance_criteria>
  <verify>
    <automated>cd admin; npm run lint</automated>
  </verify>
  <done>Admin data layer can call and type-check cleanup preview/delete APIs without breaking existing order flows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build guarded cleanup panel on admin Orders page</name>
  <files>admin/app/(admin)/orders/page.tsx</files>
  <read_first>
    - admin/app/(admin)/orders/page.tsx
    - admin/lib/api.ts
    - admin/components/data-table.tsx
  </read_first>
  <behavior>
    - Admin can set status/age filters and preview candidate counts.
    - Delete action stays disabled until explicit confirmation text is correct.
    - Successful cleanup surfaces result counts and refreshes order table data.
  </behavior>
  <action>
    Update admin/app/(admin)/orders/page.tsx by adding a cleanup control card above the DataTable with: filter controls (statuses + min_age_minutes), a Preview button, preview summary details, explicit confirmation input (for example exact phrase `DELETE {count}`), and a destructive action button tied to `ordersApi.deleteStuck`. Use React Query for preview fetch/mutation and invalidate `admin-orders` plus preview query keys after successful cleanup. Show success/error feedback with existing toast pattern, and keep the default orders list/polling behavior intact.
  </action>
  <acceptance_criteria>
    - Orders page renders a cleanup panel with status + age controls.
    - Delete button is disabled until explicit confirmation requirement is met.
    - On successful delete, UI displays deleted/failed/blocked counters and refreshes orders list query.
  </acceptance_criteria>
  <verify>
    <automated>cd admin; npm run lint</automated>
  </verify>
  <done>Orders page includes a safe, explicit, and operationally clear cleanup flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add focused Orders page tests for confirmation gate and cleanup mutation</name>
  <files>admin/app/(admin)/orders/page.test.tsx</files>
  <read_first>
    - admin/app/(admin)/orders/page.tsx
    - admin/app/(admin)/orders/[id]/page.test.tsx
    - admin/test/setup.ts
  </read_first>
  <behavior>
    - Tests verify delete action remains disabled before explicit confirmation.
    - Tests verify mutation is called with normalized filter payload once confirmed.
    - Tests verify preview and result summaries render expected counts.
  </behavior>
  <action>
    Create admin/app/(admin)/orders/page.test.tsx using the existing Vitest + Testing Library mock style from order detail tests. Mock React Query hooks and ordersApi calls to cover: preview result rendering, confirmation-gated delete button enablement, and mutation payload assertions for selected filters/age. Include at least one failure/result-reporting assertion to protect AOC-05 behavior.
  </action>
  <acceptance_criteria>
    - admin/app/(admin)/orders/page.test.tsx exists and runs under current vitest config.
    - Test suite includes a confirmation gate scenario and a delete mutation call scenario.
    - Tests assert displayed cleanup result counters or failure summary text.
  </acceptance_criteria>
  <verify>
    <automated>cd admin; npm run test -- "app/(admin)/orders/page.test.tsx"</automated>
  </verify>
  <done>Admin cleanup UX behavior is covered by deterministic page-level regression tests.</done>
</task>

</tasks>

<verification>
- Run admin lint and focused Orders page cleanup tests.
- Confirm delete action cannot execute without explicit confirmation text.
- Confirm successful cleanup invalidates and refreshes orders list data.
</verification>

<success_criteria>
- AOC-04 satisfied by guarded cleanup UI flow with explicit confirmation.
- AOC-05 satisfied by admin test coverage for confirmation and deletion result reporting.
</success_criteria>

<output>
After completion, create .planning/phases/02-clear-stuck-orders-by-deleting-them-from-the-server/02-02-SUMMARY.md
</output>