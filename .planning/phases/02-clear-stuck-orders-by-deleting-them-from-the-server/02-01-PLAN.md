---
phase: 02-clear-stuck-orders-by-deleting-them-from-the-server
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/cmd/server/main.go
  - backend/internal/handlers/admin_handler.go
  - backend/internal/services/appwrite_service.go
  - backend/internal/handlers/admin_handler_test.go
autonomous: true
requirements:
  - AOC-01
  - AOC-02
  - AOC-03
  - AOC-05
must_haves:
  truths:
    - Admin can preview stale order candidates filtered by status and age.
    - Admin can trigger bulk hard deletion and receive deleted and failed counts.
    - Active in-progress statuses are never deleted by cleanup APIs.
  artifacts:
    - path: backend/internal/handlers/admin_handler.go
      provides: Preview and bulk delete admin cleanup handlers with eligibility guards.
    - path: backend/internal/services/appwrite_service.go
      provides: Order-specific delete wrapper used by admin cleanup flow.
    - path: backend/internal/handlers/admin_handler_test.go
      provides: Focused tests for preview filtering, deletion guards, and count reporting.
    - path: backend/cmd/server/main.go
      provides: Admin cleanup route registration under existing auth and role middleware.
  key_links:
    - from: backend/cmd/server/main.go
      to: backend/internal/handlers/admin_handler.go
      via: New admin routes for stuck preview and bulk deletion.
    - from: backend/internal/handlers/admin_handler.go
      to: backend/internal/services/appwrite_service.go
      via: ListOrders and DeleteOrder calls during cleanup execution.
    - from: backend/internal/handlers/admin_handler_test.go
      to: backend/internal/handlers/admin_handler.go
      via: Route-level verification of eligibility and delete result payloads.
---

<objective>
Add backend admin cleanup APIs that preview and bulk-delete stale stuck orders while enforcing strict status guardrails.

Purpose: Let admins clear stale server-side order clutter without risking live order operations.
Output: Authenticated admin preview + delete endpoints, wired routes, and focused automated tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-clear-stuck-orders-by-deleting-them-from-the-server/02-RESEARCH.md
@backend/cmd/server/main.go
@backend/internal/handlers/admin_handler.go
@backend/internal/services/appwrite_service.go
@backend/internal/handlers/admin_handler_test.go
@backend/pkg/appwrite/query.go

<interfaces>
From backend/internal/handlers/admin_handler.go:
```go
func (h *AdminHandler) ListOrders(c *gin.Context)
func (h *AdminHandler) ActiveOrders(c *gin.Context)
func (h *AdminHandler) CancelOrder(c *gin.Context)
```

From backend/internal/services/appwrite_service.go:
```go
func (s *AppwriteService) ListOrders(queries []string) (*appwrite.DocumentList, error)
func (s *AppwriteService) DeleteDocument(collection, id string) error
```

From backend/pkg/appwrite/query.go:
```go
func QueryEqual(attribute string, values ...interface{}) string
func QueryLessThan(attribute string, value interface{}) string
func QueryOrderDesc(attribute string) string
func QueryLimit(limit int) string
func QueryOffset(offset int) string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add normalized stuck-order filters and preview endpoint</name>
  <files>backend/internal/handlers/admin_handler.go</files>
  <read_first>
    - backend/internal/handlers/admin_handler.go
    - backend/internal/models/order.go
    - backend/pkg/appwrite/query.go
  </read_first>
  <behavior>
    - Preview endpoint accepts status and min_age_minutes filters and returns stale candidate orders.
    - Requested active statuses are ignored/blocked from eligibility (AOC-03).
    - Response includes enough summary metadata for admin UI confirmation flow.
  </behavior>
  <action>
    In backend/internal/handlers/admin_handler.go add shared helper logic for cleanup filtering: normalize requested statuses, enforce allowed deletion statuses (terminal only), parse minimum age, and build Appwrite queries with status + placed_at cutoff. Add a new admin handler method `PreviewStuckOrders` at GET /admin/orders/stuck/preview that supports `statuses`, `min_age_minutes`, `page`, and `per_page`, then returns candidate documents and filter metadata via response envelope.
  </action>
  <acceptance_criteria>
    - backend/internal/handlers/admin_handler.go contains a new PreviewStuckOrders handler.
    - PreviewStuckOrders applies status and age filters using Appwrite query helpers.
    - Preview response includes both candidate rows and normalized filter context (effective statuses, blocked/ignored statuses, min_age_minutes).
  </acceptance_criteria>
  <verify>
    <automated>cd backend; go test ./internal/handlers -run TestAdminStuckOrdersPreview -count=1</automated>
  </verify>
  <done>Preview API is available and returns only stale cleanup candidates under guardrail rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bulk hard-delete endpoint and route wiring</name>
  <files>backend/internal/services/appwrite_service.go, backend/internal/handlers/admin_handler.go, backend/cmd/server/main.go</files>
  <read_first>
    - backend/internal/services/appwrite_service.go
    - backend/internal/handlers/admin_handler.go
    - backend/cmd/server/main.go
  </read_first>
  <behavior>
    - Bulk endpoint uses same normalization logic as preview to prevent filter mismatch.
    - Endpoint hard-deletes eligible stale orders and reports deleted/failed/blocked counts.
    - Route is mounted under existing admin auth + role middleware.
  </behavior>
  <action>
    Add `DeleteOrder(id string) error` to backend/internal/services/appwrite_service.go using `models.CollectionOrders`. In backend/internal/handlers/admin_handler.go add `DeleteStuckOrders` handler (POST /admin/orders/stuck/delete) that accepts statuses + min_age_minutes + optional limit, fetches candidates using shared cleanup filter helpers, blocks active statuses per AOC-03, deletes only eligible orders, and returns a structured result payload with `examined_count`, `eligible_count`, `deleted_count`, `failed_count`, `blocked_count`, and failure details per order. Register new preview and delete routes in backend/cmd/server/main.go under the admin orders routes.
  </action>
  <acceptance_criteria>
    - backend/internal/services/appwrite_service.go exports DeleteOrder.
    - backend/internal/handlers/admin_handler.go exports DeleteStuckOrders and uses DeleteOrder in a loop with result counters.
    - backend/cmd/server/main.go includes GET /admin/orders/stuck/preview and POST /admin/orders/stuck/delete routes.
    - DeleteStuckOrders never calls DeleteOrder for blocked active statuses.
  </acceptance_criteria>
  <verify>
    <automated>cd backend; go test ./internal/handlers -run TestAdminDeleteStuckOrders -count=1</automated>
  </verify>
  <done>Bulk cleanup endpoint performs guarded hard deletes and reports deterministic deletion outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add focused backend tests for preview, guards, and count reporting</name>
  <files>backend/internal/handlers/admin_handler_test.go</files>
  <read_first>
    - backend/internal/handlers/admin_handler_test.go
    - backend/internal/testutil/test_env.go
    - backend/internal/testutil/fake_appwrite.go
  </read_first>
  <behavior>
    - Tests prove preview respects age + status filtering.
    - Tests prove active statuses are blocked and remain in storage after delete call.
    - Tests prove delete response exposes deleted/failed/blocked counters.
  </behavior>
  <action>
    Extend backend/internal/handlers/admin_handler_test.go by registering new admin cleanup routes in test router and adding targeted tests: one for preview filtering by status and age cutoff, and one for bulk deletion with mixed statuses where active/in-progress orders are blocked. Assert both HTTP behavior and persisted fake Appwrite state (eligible orders removed, blocked orders retained), plus response counters and failure-reason payloads.
  </action>
  <acceptance_criteria>
    - admin_handler_test.go contains tests named for stuck preview and delete behavior.
    - Tests assert deletion counts and blocked-count semantics.
    - Tests assert blocked status orders still exist after cleanup.
  </acceptance_criteria>
  <verify>
    <automated>cd backend; go test ./internal/handlers -run TestAdminStuckOrdersPreview -count=1; go test ./internal/handlers -run TestAdminDeleteStuckOrders -count=1</automated>
  </verify>
  <done>Backend cleanup API behavior is regression-protected with focused automated tests.</done>
</task>

</tasks>

<verification>
- Run focused backend cleanup tests for preview and delete behavior.
- Confirm route-level role guard remains admin/super_admin only.
- Confirm active statuses are blocked and never physically deleted.
</verification>

<success_criteria>
- AOC-01 satisfied by preview endpoint with status + age filters.
- AOC-02 satisfied by bulk hard-delete endpoint returning deletion and failure counts.
- AOC-03 satisfied by enforced active-status guardrails.
- AOC-05 partially satisfied by backend test coverage.
</success_criteria>

<output>
After completion, create .planning/phases/02-clear-stuck-orders-by-deleting-them-from-the-server/02-01-SUMMARY.md
</output>