---
phase: 01-admin-order-detail-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/handlers/admin_handler.go
  - backend/internal/services/appwrite_service.go
  - backend/internal/models/order.go
  - backend/internal/handlers/admin_handler_test.go
autonomous: true
requirements:
  - AORD-04
  - AORD-05
must_haves:
  truths:
    - Admin order detail API returns customer identity fields for eligible orders.
    - Admin order detail API returns delivery address and coordinate fields for eligible orders.
    - Existing order response keys remain available.
  artifacts:
    - path: backend/internal/handlers/admin_handler.go
      provides: Enriched GetOrder response composition.
    - path: backend/internal/models/order.go
      provides: Stable optional enriched field contracts.
    - path: backend/internal/handlers/admin_handler_test.go
      provides: Regression coverage for enriched response and fallback behavior.
  key_links:
    - from: backend/internal/handlers/admin_handler.go
      to: backend/internal/services/appwrite_service.go
      via: GetUser and GetAddress lookups in GetOrder path.
    - from: backend/internal/handlers/admin_handler.go
      to: backend/cmd/server/main.go
      via: Admin route GET /admin/orders/:id.
---

<objective>
Deliver backend contract enrichment for admin order detail so one API call returns customer identity plus delivery location while preserving existing order payload behavior.

Purpose: Remove data gaps that prevent admin UI from showing full order context.
Output: Enriched and tested GET /api/v1/admin/orders/:id response contract.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-admin-order-detail-enrichment/01-CONTEXT.md
@.planning/phases/01-admin-order-detail-enrichment/01-RESEARCH.md
@backend/internal/handlers/admin_handler.go
@backend/internal/services/appwrite_service.go
@backend/internal/models/order.go
@backend/internal/handlers/admin_handler_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and document enriched admin order detail contract</name>
  <files>backend/internal/models/order.go, backend/internal/handlers/admin_handler.go</files>
  <read_first>
    - backend/internal/models/order.go
    - backend/internal/handlers/admin_handler.go
    - backend/internal/services/appwrite_service.go
    - backend/cmd/server/main.go
  </read_first>
  <behavior>
    - Existing order fields stay unchanged in successful GET /admin/orders/:id responses.
    - Response can include customer_name, customer_phone, customer_email, delivery_address_line, delivery_city, delivery_latitude, delivery_longitude.
    - Missing customer/address source docs do not fail whole request; enriched fields remain absent or empty.
  </behavior>
  <action>
    Update backend/internal/models/order.go to add optional enriched fields on the Order struct with explicit json tags: customer_name, customer_phone, customer_email, delivery_address_line, delivery_city, delivery_latitude, delivery_longitude. In backend/internal/handlers/admin_handler.go update GetOrder to: load order doc, read customer_id and delivery_address_id, fetch user with h.appwrite.GetUser(customerID) and address with h.appwrite.GetAddress(addressID), map only the listed enriched fields into response doc, and preserve all existing order keys and current success/error response behavior.
  </action>
  <acceptance_criteria>
    - backend/internal/models/order.go contains json tags customer_name and customer_phone.
    - backend/internal/models/order.go contains json tags delivery_address_line and delivery_latitude.
    - backend/internal/handlers/admin_handler.go GetOrder includes calls to h.appwrite.GetUser and h.appwrite.GetAddress.
    - backend/internal/handlers/admin_handler.go still returns utils.Success for found orders and utils.NotFound for missing order.
  </acceptance_criteria>
  <verify>
    <automated>cd backend; go test ./internal/handlers -run TestAdminGetOrder -count=1</automated>
  </verify>
  <done>GetOrder returns enriched customer and address fields for available linked docs without breaking existing fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add focused handler tests for enriched response and fallback cases</name>
  <files>backend/internal/handlers/admin_handler_test.go</files>
  <read_first>
    - backend/internal/handlers/admin_handler_test.go
    - backend/internal/handlers/admin_handler.go
    - backend/internal/services/fake_appwrite.go
  </read_first>
  <behavior>
    - Test passes when order has valid customer_id and delivery_address_id, asserting enriched values are present.
    - Test passes when user or address lookup fails, asserting endpoint still returns order payload and does not 500.
  </behavior>
  <action>
    In backend/internal/handlers/admin_handler_test.go add or extend TestAdminGetOrder variants that seed order docs with customer_id and delivery_address_id, seed user/address docs with expected values, invoke GET /api/v1/admin/orders/:id, and assert response data includes customer_name/customer_phone and delivery_address_line/delivery_latitude. Add fallback case where linked docs are absent and assert status 200 with base order fields still present.
  </action>
  <acceptance_criteria>
    - backend/internal/handlers/admin_handler_test.go contains TestAdminGetOrder with assertions for customer_name and delivery_address_line.
    - backend/internal/handlers/admin_handler_test.go contains a missing-linked-doc fallback assertion that still expects 200.
    - No existing admin order tests are removed.
  </acceptance_criteria>
  <verify>
    <automated>cd backend; go test ./internal/handlers -run TestAdminGetOrder -count=1</automated>
  </verify>
  <done>Backend has deterministic tests proving enriched contract behavior and safe fallback behavior.</done>
</task>

</tasks>

<verification>
- Run focused backend handler test suite for admin order detail.
- Confirm response includes enriched keys when source docs exist.
- Confirm fallback path preserves successful response without enriched data.
</verification>

<success_criteria>
- AORD-04 satisfied by enriched backend response contract.
- AORD-05 partially satisfied via backend fallback behavior tests.
</success_criteria>

<output>
After completion, create .planning/phases/01-admin-order-detail-enrichment/01-01-SUMMARY.md
</output>
