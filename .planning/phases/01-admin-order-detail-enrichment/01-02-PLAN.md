---
phase: 01-admin-order-detail-enrichment
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - admin/types/index.ts
  - admin/app/(admin)/orders/[id]/page.tsx
  - admin/app/(admin)/orders/[id]/page.test.tsx
autonomous: true
requirements:
  - AORD-01
  - AORD-02
  - AORD-03
  - AORD-05
must_haves:
  truths:
    - Admin sees customer name and phone in order detail view.
    - Admin sees delivery location details and map coordinate context when available.
    - Missing fields render explicit fallback labels instead of blank or broken UI.
  artifacts:
    - path: admin/types/index.ts
      provides: Optional enriched order field typing.
    - path: admin/app/(admin)/orders/[id]/page.tsx
      provides: Customer/location UI rendering from enriched contract.
    - path: admin/app/(admin)/orders/[id]/page.test.tsx
      provides: Rendering and fallback regression coverage.
  key_links:
    - from: admin/lib/api.ts
      to: admin/app/(admin)/orders/[id]/page.tsx
      via: ordersApi.get query returning enriched order data.
    - from: backend/internal/handlers/admin_handler.go
      to: admin/types/index.ts
      via: response keys mapped into typed order model.
---

<objective>
Render complete customer and delivery location details on the admin order detail page using the enriched backend contract.

Purpose: Ensure admins can act on full order context directly from one page.
Output: Typed and tested order detail UI displaying customer identity and location details with fallbacks.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-admin-order-detail-enrichment/01-CONTEXT.md
@.planning/phases/01-admin-order-detail-enrichment/01-RESEARCH.md
@.planning/phases/01-admin-order-detail-enrichment/01-01-SUMMARY.md
@admin/types/index.ts
@admin/app/(admin)/orders/[id]/page.tsx
@admin/lib/api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend admin order typing for enriched customer and location fields</name>
  <files>admin/types/index.ts</files>
  <read_first>
    - admin/types/index.ts
    - admin/app/(admin)/orders/[id]/page.tsx
    - .planning/phases/01-admin-order-detail-enrichment/01-01-SUMMARY.md
  </read_first>
  <behavior>
    - Order type exposes optional customer_name, customer_phone, customer_email.
    - Order type exposes optional delivery_address_line, delivery_city, delivery_latitude, delivery_longitude.
  </behavior>
  <action>
    Update admin/types/index.ts Order interface by adding optional enriched fields exactly matching backend contract keys: customer_name?: string, customer_phone?: string, customer_email?: string, delivery_address_line?: string, delivery_city?: string, delivery_latitude?: number, delivery_longitude?: number. Keep all existing fields unchanged.
  </action>
  <acceptance_criteria>
    - admin/types/index.ts contains customer_name?: string and customer_phone?: string in Order interface.
    - admin/types/index.ts contains delivery_address_line?: string and delivery_latitude?: number in Order interface.
    - Existing Order properties are still present.
  </acceptance_criteria>
  <verify>
    <automated>cd admin; npm run lint</automated>
  </verify>
  <done>Admin type layer supports enriched order detail fields with no breaking type changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render customer identity and location details with safe fallbacks</name>
  <files>admin/app/(admin)/orders/[id]/page.tsx</files>
  <read_first>
    - admin/app/(admin)/orders/[id]/page.tsx
    - admin/types/index.ts
    - admin/components/ui/status-badge.tsx
  </read_first>
  <behavior>
    - Customer card shows customer name row and phone row; phone row uses tel link when value exists.
    - Customer card shows location/address row from enriched delivery fields.
    - If values missing, card shows Not available fallback text and does not crash.
  </behavior>
  <action>
    Update admin/app/(admin)/orders/[id]/page.tsx Customer InfoCard section to render: Name row from order.customer_name fallback Not available, Phone row from order.customer_phone as clickable tel link fallback Not available, Address row from order.delivery_address_line plus order.delivery_city fallback Not available, and Coordinates row from order.delivery_latitude and order.delivery_longitude fallback Not available. Keep existing customer_id row for traceability.
  </action>
  <acceptance_criteria>
    - admin/app/(admin)/orders/[id]/page.tsx contains Row label Name referencing order.customer_name.
    - admin/app/(admin)/orders/[id]/page.tsx contains Row label Phone with tel: link.
    - admin/app/(admin)/orders/[id]/page.tsx contains Row label Address and a coordinates display branch.
    - Fallback string Not available appears for missing enriched fields.
  </acceptance_criteria>
  <verify>
    <automated>cd admin; npm run lint</automated>
  </verify>
  <done>Admin order detail page visibly shows customer identity and location context with robust fallback handling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add UI regression tests for enriched rendering and fallback behavior</name>
  <files>admin/app/(admin)/orders/[id]/page.test.tsx</files>
  <read_first>
    - admin/app/(admin)/orders/[id]/page.tsx
    - admin/app/(admin)/orders/page.tsx
    - admin/package.json
  </read_first>
  <behavior>
    - Test asserts name/phone/address rows render when enriched fields are present.
    - Test asserts fallback text renders when enriched fields are absent.
  </behavior>
  <action>
    Create or update admin/app/(admin)/orders/[id]/page.test.tsx using current admin test stack. Mock ordersApi.get response with enriched fields for positive case and missing fields for fallback case. Assert rendered text contains customer name, customer phone, and address for positive scenario, and contains Not available fallback rows for missing-field scenario.
  </action>
  <acceptance_criteria>
    - admin/app/(admin)/orders/[id]/page.test.tsx exists and contains at least two scenarios: enriched data and fallback data.
    - Assertions include customer_name, customer_phone, and Not available fallback checks.
  </acceptance_criteria>
  <verify>
    <automated>cd admin; npm run test -- --runInBand orders/[id]/page.test.tsx</automated>
  </verify>
  <done>UI behavior for enriched order details and fallback behavior is covered by automated tests.</done>
</task>

</tasks>

<verification>
- Run admin lint and focused order detail tests.
- Confirm route /orders/:id displays customer name, phone, and location fields from enriched API response.
- Confirm fallback content appears for missing enriched fields.
</verification>

<success_criteria>
- AORD-01, AORD-02, AORD-03 satisfied by visible order detail rows.
- AORD-05 satisfied by fallback rendering and UI regression checks.
</success_criteria>

<output>
After completion, create .planning/phases/01-admin-order-detail-enrichment/01-02-SUMMARY.md
</output>
